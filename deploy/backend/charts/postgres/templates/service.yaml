apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgres-java
  name: postgres-java
  namespace: {{ .Release.Namespace }}
spec:
  clusterIP: None
  ports:
    - port: 5432
      name: postgres-java
  selector:
    app: postgres-java
