spring:
    datasource:
        url:
        username:
        password:
        testWhileIdle: true
        validationQuery: SELECT 1
        driver: org.postgesql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect

server:
    port: 8080

